# Launch Reports

Generated launch-readiness reports are written here by:

- `npm run eval:itineraries -- --fixtures`
- `npm run eval:itineraries -- --live`
- `npm run check:launch`

JSON and Markdown reports are intentionally ignored so deploy checks do not create noisy commits.
